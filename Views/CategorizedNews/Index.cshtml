@model List<News>

</News>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/home-index.css">
</head>

<style>
    h1{margin: 20px;}
</style>

<body>

    @if (Model.Count == 0)
        {
            <h2>Bu kategoride haber bulunmamaktadır.</h2>
        }
        else
        {   
            <h1>@Model.First().Category!.CategoryName Haberleri</h1>
            <br>
            @foreach (var news in Model)
            {
                               
                <div class="news-card">
                    @await Html.PartialAsync("_NewsCard", news)    
                </div>
               
            }
        }
    
</body>

</html>